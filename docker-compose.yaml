services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
#    deploy:
#      resources:
#        limits:
#          cpus: "2.0"
#          memory: "1g"
